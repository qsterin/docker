version: '3.1'

services:

  db:
    image: postgres:alpine
    ports:
      - "5432:5432"
    volumes:
      - .data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=APP
      - POSTGRES_USER=APP
      - POSTGRES_PASSWORD=9mREsvXDs9Gk89Ef

